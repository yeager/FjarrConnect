name: FjarrConnect

options:
  bundleIdPrefix: se.fjarrconnect
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

packages:
  RoyalVNCKit:
    # Separates macOS VNC reads and writes so pending updates cannot stall keyboard input.
    url: https://github.com/yeager/royalvnc
    revision: "efdf33d766a053ea39f945fb50c1b22986bf47e2"
  SwiftTerm:
    url: https://github.com/migueldeicaza/SwiftTerm
    exactVersion: "1.10.1"

targets:
  FjarrConnect:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - App
      - Model
      - Protocols
      - Discovery
      - Views
      - Resources
    dependencies:
      - package: RoyalVNCKit
        embed: true
      - package: SwiftTerm
    info:
      path: Generated/Info.plist
      properties:
        CFBundleDisplayName: FjärrConnect          # the ö lives here, in the display name
        CFBundleName: FjarrConnect
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        LSMinimumSystemVersion: "14.0"
        LSApplicationCategoryType: public.app-category.utilities
        NSHumanReadableCopyright: "Copyright © 2026 Daniel Nylander"
        NSLocalNetworkUsageDescription: "Find VNC, RDP and SSH servers on your local network."
        NSMicrophoneUsageDescription: "Share this Mac's microphone with an RDP session when you enable microphone redirection."
        NSBonjourServices:
          - _rfb._tcp
          - _rdp._tcp
          - _ssh._tcp
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: se.fjarrconnect.app
        PRODUCT_NAME: FjarrConnect
        MARKETING_VERSION: "0.2.29"
        CURRENT_PROJECT_VERSION: "31"
        SWIFT_VERSION: "5.0"
        ARCHS: "$(NATIVE_ARCH_ACTUAL)"
        ONLY_ACTIVE_ARCH: NO
        ENABLE_APP_SANDBOX: NO
        ENABLE_HARDENED_RUNTIME: YES
        CODE_SIGN_STYLE: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon

  FjarrConnectTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "14.0"
    sources: [Tests]
    dependencies:
      - target: FjarrConnect
      - package: RoyalVNCKit
        embed: false
      - package: SwiftTerm
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "5.0"

  FjarrConnectUITests:
    type: bundle.ui-testing
    platform: macOS
    deploymentTarget: "14.0"
    sources: [UITests]
    dependencies:
      - target: FjarrConnect
    settings:
      base:
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_VERSION: "5.0"

schemes:
  FjarrConnect:
    build:
      targets:
        FjarrConnect: all
    test:
      targets: [FjarrConnectTests, FjarrConnectUITests]
    run:
      config: Debug
    archive:
      config: Release
