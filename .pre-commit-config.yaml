# Local secret scanning before every commit.
# One-time setup:
#   pipx install pre-commit   # or: brew install pre-commit
#   pre-commit install
# Then gitleaks runs automatically on `git commit`.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
